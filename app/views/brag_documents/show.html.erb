<div class="min-h-screen bg-gradient-to-br from-blue-50 via-indigo-50 to-purple-50">
  <div class="container mx-auto px-4 py-12">
    <!-- Header Section -->
    <div class="text-center mb-12">
      <div class="relative inline-block">
        <h1 class="text-4xl font-bold bg-gradient-to-r from-blue-600 via-purple-600 to-pink-600 bg-clip-text text-transparent mb-5">
          ✨ My Brag Document✨
        </h1>
        <div class="absolute -top-4 -right-4 text-4xl animate-bounce">🎯</div>
      </div>
      <p class="text-xl text-gray-600 max-w-2xl mx-auto leading-relaxed">
        "Every accomplishment starts with the decision to try" - ติดตามความสำเร็จในเส้นทางสู่ความฝันของฉัน
      </p>
    </div>

    <!-- Goals Overview Cards -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
      <!-- English Exam Goal -->
      <div class="bg-white rounded-2xl shadow-lg overflow-hidden transform hover:scale-105 transition-transform duration-300">
        <div class="bg-gradient-to-r from-green-400 to-blue-500 h-32 relative">
          <div class="absolute inset-0 flex items-center justify-center">
            <div class="text-6xl">📚</div>
          </div>
        </div>
        <div class="p-6">
          <h3 class="text-xl font-bold text-gray-800 mb-2">English Proficiency</h3>
          <p class="text-gray-600">เป้าหมายสอบภาษาอังกฤษ IELTS/TOEIC</p>
        </div>
      </div>

      <!-- Pilot Exam Goal -->
      <div class="bg-white rounded-2xl shadow-lg overflow-hidden transform hover:scale-105 transition-transform duration-300">
        <div class="bg-gradient-to-r from-blue-400 to-purple-500 h-32 relative">
          <div class="absolute inset-0 flex items-center justify-center">
            <div class="text-6xl">✈️</div>
          </div>
        </div>
        <div class="p-6">
          <h3 class="text-xl font-bold text-gray-800 mb-2">Pilot License</h3>
          <p class="text-gray-600">เป้าหมายสอบใบอนุญาตนักบิน PPL/CPL</p>
        </div>
      </div>

      <!-- SM Goal -->
      <div class="bg-white rounded-2xl shadow-lg overflow-hidden transform hover:scale-105 transition-transform duration-300">
        <div class="bg-gradient-to-r from-purple-400 to-pink-500 h-32 relative">
          <div class="absolute inset-0 flex items-center justify-center">
            <div class="text-6xl">👨‍💼</div>
          </div>
        </div>
        <div class="p-6">
          <h3 class="text-xl font-bold text-gray-800 mb-2">Scrum Master</h3>
          <p class="text-gray-600">เป้าหมายเป็น SM ครั้งแรกที่ทำให้ดีที่สุด</p>
        </div>
      </div>
    </div>

    <!-- Completed Quests Section -->
    <div class="mb-8">
      <h2 class="text-3xl font-bold text-center mb-8 text-gray-800">
        🏆 Completed Achievements
      </h2>
      
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
        <% @completed_quests.each_with_index do |quest, index| %>
          <div class="bg-white rounded-2xl shadow-lg overflow-hidden transform hover:scale-105 transition-all duration-300 hover:shadow-xl">
            <!-- Achievement Badge -->
            <div class="bg-gradient-to-r from-yellow-400 via-orange-500 to-red-500 h-20 relative">
              <div class="absolute inset-0 flex items-center justify-center">
                <div class="text-3xl">
                  <%= ['🌟', '🎯', '💎', '🔥', '⚡', '🚀', '👑', '🏅'][index % 8] %>
                </div>
              </div>
              <div class="absolute top-2 right-2 bg-white rounded-full px-2 py-1">
                <span class="text-xs font-bold text-orange-600">#<%= index + 1 %></span>
              </div>
            </div>
            
            <!-- Content -->
            <div class="p-6">
              <h3 class="text-lg font-bold mb-3 text-gray-800 line-clamp-2">
                <%= quest.title %>
              </h3>
              <p class="text-gray-600 text-sm mb-4 line-clamp-3">
                <%= quest.description %>
              </p>
              
              <!-- Achievement Stats -->
              <div class="flex items-center justify-between text-xs text-gray-500 mb-4">
                <div class="flex items-center">
                  <span class="w-2 h-2 bg-green-400 rounded-full mr-2"></span>
                  Completed
                </div>
                <span><%= quest.updated_at.to_date.strftime("%d %b %Y") %></span>
              </div>
              
              <!-- Achievement Type Badge -->
              <div class="inline-block">
                <span class="bg-gradient-to-r from-blue-100 to-purple-100 text-blue-800 text-xs font-semibold px-3 py-1 rounded-full">
                  Achievement Unlocked
                </span>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Motivational Section -->
    <div class="bg-white rounded-2xl shadow-lg p-8 mb-8 text-center">
      <div class="text-4xl mb-4">🌈</div>
      <h3 class="text-2xl font-bold text-gray-800 mb-4">Keep Pushing Forward!</h3>
      <p class="text-gray-600 mb-6">
        "Success is not final, failure is not fatal: it is the courage to continue that counts." - Winston Churchill
      </p>
      <div class="flex justify-center items-center space-x-4 text-sm text-gray-500">
        <span class="flex items-center">
          <span class="w-3 h-3 bg-green-400 rounded-full mr-2"></span>
          <%= @completed_quests.count %> Achievements
        </span>
        <span class="flex items-center">
          <span class="w-3 h-3 bg-blue-400 rounded-full mr-2"></span>
          3 Active Goals
        </span>
        <span class="flex items-center">
          <span class="w-3 h-3 bg-purple-400 rounded-full mr-2"></span>
          Journey Continues...
        </span>
      </div>
    </div>

    <!-- Navigation -->
    <div class="text-center">
      <%= link_to quests_path, class: "inline-flex items-center px-8 py-3 bg-gradient-to-r from-blue-500 to-purple-600 hover:from-blue-600 hover:to-purple-700 text-white font-bold rounded-full transform hover:scale-105 transition-all duration-300 shadow-lg hover:shadow-xl" do %>
        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
        </svg>
        Back to Quest Hub
      <% end %>
    </div>

    <!-- Floating Action Button for New Quest -->
    <div class="fixed bottom-8 right-8">
      <button class="bg-gradient-to-r from-pink-500 to-purple-600 hover:from-pink-600 hover:to-purple-700 text-white p-4 rounded-full shadow-lg hover:shadow-xl transform hover:scale-110 transition-all duration-300">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
        </svg>
      </button>
    </div>
  </div>
</div>

<!-- Add custom CSS for line-clamp if not available -->
<style>
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  
  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  
  .animate-bounce {
    animation: bounce 2s infinite;
  }
  
  @keyframes bounce {
    0%, 20%, 53%, 80%, 100% {
      transform: translate3d(0,0,0);
    }
    40%, 43% {
      transform: translate3d(0,-15px,0);
    }
    70% {
      transform: translate3d(0,-7px,0);
    }
    90% {
      transform: translate3d(0,-2px,0);
    }
  }
</style>