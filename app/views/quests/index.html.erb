<div class="min-h-screen bg-gray-50">
  <!-- เปลี่ยนจาก container mx-auto เป็น w-full -->
  <div class="w-full px-3 sm:px-6 lg:container lg:mx-auto lg:px-8 py-6 sm:py-8">
    
    <!-- Header with Image Section -->
    <div class="bg-white rounded-2xl shadow-2xl overflow-hidden mb-8">
      <!-- Hero Image Section - Taller -->
      <div class="relative h-48 sm:h-56 lg:h-64 bg-gradient-to-r from-blue-400 via-purple-500 to-pink-500 flex items-center justify-center">
        <div class="text-center text-white px-4">
          <div class="text-6xl sm:text-5xl lg:text-7xl animate-bounce">🎯</div>
          <h1 class="text-6xl sm:text-5xl lg:text-7xl font-bold mb-2">Kritsada Mahanam</h1>
          <p class="text-2xl sm:text-2xl lg:text-4xl opacity-90">My Academy Quests</p>
        </div>
      </div>
      
      <!-- Profile Image - Full Screen Size -->
        <div class="flex justify-center -mt-6 sm:-mt-4 lg:-mt-6 relative z-10">
          <div class="relative group w-full max-w-md">
            <!-- Enhanced Glow Effect Background -->
            <div class="absolute inset-0 bg-gradient-to-r from-blue-400 via-purple-500 to-pink-500 rounded-full blur-2xl opacity-40 group-hover:opacity-70 transition-all duration-500 scale-125 animate-pulse"></div>
            <div class="absolute inset-0 bg-gradient-to-r from-pink-300 via-yellow-300 to-blue-300 rounded-full blur-3xl opacity-20 group-hover:opacity-40 transition-all duration-700 scale-150"></div>
            
            <!-- Main Profile Image - Nearly Full Screen -->
            <div class="relative w-72 h-72 sm:w-80 sm:h-80 lg:w-96 lg:h-96 xl:w-[28rem] xl:h-[28rem] mx-auto rounded-full border-8 sm:border-12 lg:border-16 border-white shadow-[0_20px_60px_rgba(0,0,0,0.3)] overflow-hidden bg-white transform group-hover:scale-110 transition-all duration-500 hover:border-yellow-300">
              <%= image_tag "me.png", class: "w-full h-full object-cover group-hover:scale-110 transition-transform duration-700" %>
              
              <!-- Overlay gradient on hover -->
              <div class="absolute inset-0 bg-gradient-to-t from-purple-600/30 via-transparent to-yellow-400/20 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
              
              <!-- Sparkle Effects -->
              <div class="absolute top-8 left-8 text-4xl opacity-0 group-hover:opacity-100 transition-all duration-500 animate-bounce delay-100">✨</div>
              <div class="absolute top-12 right-16 text-3xl opacity-0 group-hover:opacity-100 transition-all duration-700 animate-pulse delay-300">💫</div>
              <div class="absolute bottom-16 left-12 text-3xl opacity-0 group-hover:opacity-100 transition-all duration-600 animate-bounce delay-500">🌟</div>
              <div class="absolute bottom-8 right-8 text-2xl opacity-0 group-hover:opacity-100 transition-all duration-500 animate-pulse delay-700">⭐</div>
            </div>
            
            <!-- Multiple Achievement Rings -->
            <div class="absolute -top-4 -right-4 w-16 h-16 sm:w-20 sm:h-20 bg-gradient-to-r from-yellow-400 to-orange-500 rounded-full flex items-center justify-center text-3xl sm:text-4xl shadow-2xl animate-bounce z-20">
              👑
            </div>
            
            <div class="absolute -bottom-6 -left-6 w-14 h-14 sm:w-16 sm:h-16 bg-gradient-to-r from-green-400 to-blue-500 rounded-full flex items-center justify-center text-2xl sm:text-3xl shadow-2xl animate-pulse z-20">
              🏆
            </div>
            
            <div class="absolute top-8 -left-4 w-12 h-12 sm:w-14 sm:h-14 bg-gradient-to-r from-pink-400 to-purple-500 rounded-full flex items-center justify-center text-xl sm:text-2xl shadow-2xl animate-bounce delay-1000 z-20">
              🎯
            </div>
            
            <!-- Floating Text Around Image -->
            <div class="absolute -top-8 left-1/2 transform -translate-x-1/2 text-center opacity-0 group-hover:opacity-100 transition-all duration-500 delay-200">
              <div class="bg-white/90 backdrop-blur-sm rounded-full px-4 py-2 shadow-lg">
                <span class="text-lg font-bold text-gray-800">Quest Master</span>
              </div>
            </div>
            
            <div class="absolute -bottom-12 left-1/2 transform -translate-x-1/2 text-center opacity-0 group-hover:opacity-100 transition-all duration-500 delay-400">
              <div class="bg-gradient-to-r from-blue-500 to-purple-600 text-white rounded-full px-6 py-3 shadow-lg">
                <span class="text-xl font-bold">Pleum</span>
              </div>
            </div>
          </div>
        </div>

      <!-- Floating Particles Background -->
      <div class="absolute inset-0 overflow-hidden pointer-events-none">
        <div class="absolute top-1/4 left-1/4 w-2 h-2 bg-yellow-400 rounded-full animate-ping opacity-75"></div>
        <div class="absolute top-1/3 right-1/4 w-1 h-1 bg-blue-400 rounded-full animate-pulse opacity-50"></div>
        <div class="absolute bottom-1/4 left-1/3 w-3 h-3 bg-purple-400 rounded-full animate-bounce opacity-60"></div>
        <div class="absolute bottom-1/3 right-1/3 w-2 h-2 bg-pink-400 rounded-full animate-ping opacity-40 delay-500"></div>
      </div>

      <div class="p-3 sm:p-4 pt-8 sm:pt-6 lg:pt-10">
        <!-- Quest Form -->
        <div class="mb-8">
          <%= turbo_frame_tag "quest_form" do %>
            <%= render "form", quest: @quest %>
          <% end %>
        </div>

        <!-- Simple Stats - Larger and More Prominent -->
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 sm:gap-6 mb-8">
          <div class="text-center p-8 sm:p-6 bg-gradient-to-br from-blue-50 to-blue-100 rounded-2xl hover:from-blue-100 hover:to-blue-200 transition-all duration-300 shadow-lg hover:shadow-xl transform hover:scale-105 border border-blue-200">
            <div class="text-5xl sm:text-3xl lg:text-4xl font-extrabold text-blue-600 mb-2"><%= @quests.count %></div>
            <div class="text-lg sm:text-base text-blue-800 font-semibold">Total Quests</div>
          </div>
          <div class="text-center p-8 sm:p-6 bg-gradient-to-br from-green-50 to-green-100 rounded-2xl hover:from-green-100 hover:to-green-200 transition-all duration-300 shadow-lg hover:shadow-xl transform hover:scale-105 border border-green-200">
            <div class="text-5xl sm:text-3xl lg:text-4xl font-extrabold text-green-600 mb-2"><%= @quests.select(&:status).count %></div>
            <div class="text-lg sm:text-base text-green-800 font-semibold">Completed ✅</div>
          </div>
          <div class="text-center p-8 sm:p-6 bg-gradient-to-br from-orange-50 to-orange-100 rounded-2xl hover:from-orange-100 hover:to-orange-200 transition-all duration-300 shadow-lg hover:shadow-xl transform hover:scale-105 border border-orange-200">
            <div class="text-5xl sm:text-3xl lg:text-4xl font-extrabold text-orange-600 mb-2"><%= @quests.reject(&:status).count %></div>
            <div class="text-lg sm:text-base text-orange-800 font-semibold">Active 🚀</div>
          </div>
        </div>

        <!-- Brag Document Button - More Prominent -->
        <div class="w-full">
          <%= link_to brag_document_path, class: "block w-full group" do %>
            <div class="bg-gradient-to-r from-green-500 via-blue-500 to-purple-600 hover:from-green-600 hover:via-blue-600 hover:to-purple-700 text-white p-8 sm:p-6 rounded-3xl shadow-2xl hover:shadow-3xl transform hover:scale-102 transition-all duration-300 w-full border-2 border-white/20">
              <div class="flex items-center justify-center">
                <div class="text-center flex-1">
                  <div class="text-3xl sm:text-2xl lg:text-3xl font-bold mb-2 flex items-center justify-center">
                    <span class="text-4xl mr-3">📖</span>
                    Brag Document
                  </div>
                  <div class="text-lg sm:text-base opacity-90 font-medium">Showcase your epic achievements!</div>
                </div>
                <div class="text-4xl sm:text-3xl ml-6 group-hover:translate-x-3 group-hover:scale-110 transition-all duration-300">→</div>
              </div>
              
              <!-- Sparkle effects -->
              <div class="absolute top-4 left-4 text-2xl opacity-50 animate-pulse">✨</div>
              <div class="absolute bottom-4 right-4 text-2xl opacity-50 animate-pulse delay-500">⭐</div>
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <!-- Quest List - Enhanced Headers -->
    <div class="space-y-8 w-full">
      <div id="quest-list">
        <%= turbo_stream_from "quests" %>
        
        <!-- Active Quests -->
        <% if @quests.reject(&:status).any? %>
          <div class="mb-10">
            <h2 class="text-3xl sm:text-2xl lg:text-3xl font-bold mb-6 text-gray-700 flex items-center flex-wrap bg-white p-4 rounded-2xl shadow-lg">
              <span class="mr-4 text-4xl sm:text-3xl animate-bounce">🚀</span>
              <span class="flex-1 text-orange-600">Active Quests</span>
              <span class="text-lg sm:text-base bg-orange-500 text-white px-4 py-2 rounded-full font-bold shadow-md">
                <%= @quests.reject(&:status).count %>
              </span>
            </h2>
            <div class="space-y-4">
              <%= render @quests.reject(&:status) %>
            </div>
          </div>
        <% end %>
        
        <!-- Completed Quests -->
        <% if @quests.select(&:status).any? %>
          <div class="mb-10">
            <h2 class="text-3xl sm:text-2xl lg:text-3xl font-bold mb-6 text-gray-500 flex items-center flex-wrap bg-white p-4 rounded-2xl shadow-lg">
              <span class="mr-4 text-4xl sm:text-3xl">✅</span>
              <span class="flex-1 text-green-600">Completed Quests</span>
              <span class="text-lg sm:text-base bg-green-500 text-white px-4 py-2 rounded-full font-bold shadow-md">
                <%= @quests.select(&:status).count %>
              </span>
            </h2>
            <div class="opacity-80 space-y-4">
              <%= render @quests.select(&:status) %>
            </div>
          </div>
        <% end %>
        
        <!-- Empty State - Enhanced -->
        <% if @quests.empty? %>
          <div class="text-center py-20 bg-white rounded-2xl shadow-lg border-2 border-dashed border-gray-200">
            <div class="text-8xl sm:text-6xl mb-6 animate-bounce">🎯</div>
            <h3 class="text-3xl sm:text-2xl font-bold text-gray-600 mb-4">No quests yet</h3>
            <p class="text-xl sm:text-lg text-gray-500 mb-6">Your adventure starts here!</p>
            <div class="text-6xl opacity-20">🚀 ⭐ 🏆</div>
          </div>
        <% end %>
      </div>
    </div>

    <!-- Mobile Bottom Space -->
    <div class="h-8"></div>

  </div>
</div>

<!-- Enhanced Custom Styles -->
<style>
  /* Custom border widths */
  .border-12 {
    border-width: 12px;
  }
  
  .border-16 {
    border-width: 16px;
  }
  
  /* Enhanced blur effects */
  .blur-2xl {
    filter: blur(40px);
  }
  
  .blur-3xl {
    filter: blur(64px);
  }
  
  /* Floating animation */
  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-10px);
    }
  }
  
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
  
  /* Ping animation with delay */
  @keyframes ping-delayed {
    0% {
      transform: scale(1);
      opacity: 1;
    }
    75%, 100% {
      transform: scale(2);
      opacity: 0;
    }
  }
  
  .animate-ping-delayed {
    animation: ping-delayed 2s cubic-bezier(0, 0, 0.2, 1) infinite;
    animation-delay: 1s;
  }
  
  /* Enhanced hover effects */
  .group:hover .animate-bounce {
    animation-duration: 0.8s;
  }
  
  .group:hover .animate-pulse {
    animation-duration: 1.5s;
  }
  
  /* Responsive adjustments */
  @media (max-width: 640px) {
    .w-72 { width: 20rem; }
    .h-72 { height: 20rem; }
  }
  
  @media (min-width: 641px) and (max-width: 1024px) {
    .w-80 { width: 22rem; }
    .h-80 { height: 22rem; }
  }
  
  @media (min-width: 1025px) {
    .w-96 { width: 26rem; }
    .h-96 { height: 26rem; }
  }
  
  @media (min-width: 1280px) {
    .w-[28rem] { width: 28rem; }
    .h-[28rem] { height: 28rem; }
  }
</style>