<%= form_with model: quest, data: { turbo_frame: "quest_form" }, class: "bg-gradient-to-r from-blue-50 to-purple-50 p-6 rounded-2xl shadow-lg mb-6" do |f| %>
  <div class="flex flex-col sm:flex-row gap-4 items-end">
    <div class="flex-grow">
      <label class="block text-sm font-semibold text-gray-700 mb-2">
        ✨ Create New Quest
      </label>
      <%= f.text_field :title, 
          placeholder: "What's your next adventure? Type your quest here...", 
          class: "w-full rounded-xl px-4 py-3 border-2 border-gray-200 focus:outline-none focus:ring-3 focus:ring-blue-400 focus:border-transparent transition-all duration-300 text-gray-700 placeholder-gray-400 shadow-sm" %>
    </div>
    <%= f.hidden_field :status, value: false %>
    <div>
      <%= f.submit "🚀 Add Quest", 
          class: "bg-gradient-to-r from-blue-500 via-purple-500 to-pink-500 hover:from-blue-600 hover:via-purple-600 hover:to-pink-600 text-white font-bold py-3 px-6 rounded-xl shadow-lg hover:shadow-xl transform hover:scale-105 transition-all duration-300 cursor-pointer" %>
    </div>
  </div>
<% end %>